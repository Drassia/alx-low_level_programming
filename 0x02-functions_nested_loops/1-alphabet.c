#include <main.h>
/**
  * print_alphabet - Entry point
  *
  * Return: Always 0 (success)
  */
void print_alphabet(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; al++)
		_putchar(letter);

	_putchar('\n');
}
