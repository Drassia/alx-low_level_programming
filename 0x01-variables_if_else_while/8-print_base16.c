#include <stdio.h>
/**
  *main - Entry point
  *
  *Return: Always 0 (success)
  */

int main(void)
{
	char v;

	int t;

	v = 'a';
	t = 0;
	while
		(t < 10) {
			putchar(t + '0');
			t++;
		}
	while
		(v <= 'f') {
			putchar(v);
			v++;
		}
	putchar('\n');
	return (0);
}
